# ============================================================================
# filters.yaml — AUTO-GENERATED by 2_company_details_to_initial_filter.py
# ============================================================================
# Generated from company_details.json. This file is plain YAML and safe to
# hand-edit afterward; re-running the generator script will overwrite it
# completely, so save manual tweaks elsewhere if you want to keep them.
#
# Ladder cache key: 9675395e4c36a012  (see .filter_ladders_cache.json --
# if this filters.yaml ever looks wrong for the company it was built
# from, check whether this key collides with another company's entry)
#
# TOLERANCE (see script docstring for full details): every criterion below
# was produced at its own tolerance in [0.0, 1.0], the inverse of filter
# strength (0.0 = strict/true to company_details.json, 1.0 = no filtering
# on that criterion). Defaults used for this run unless overridden per
# criterion:
#   cpv=0.50  nuts=0.50  value=0.50  exclude=0.50  role_hint=0.50
# NOTE: exclude_keywords represents genuine hard capability limits (things
# the company explicitly said it cannot/will not do). A tolerance above 0.0
# there means real exclusions are being dropped -- recommended default is
# --tolerance-exclude 0.0 unless you have a specific reason to loosen it.
#
# See fetch_tenders.py / the original filters.yaml for the full field
# reference (cpv_prefixes, nuts_prefixes, value_min/max, exclude_keywords,
# role_hint_reject, and the newer optional fields).
# -----------------------------------------------------------------------------

display_name: "company"

# Exact tolerance dials used to build this file (machine-readable;
# do not remove -- downstream scripts depend on this block).
tolerance:
  cpv: 0.5000
  nuts: 0.5000
  value: 0.5000
  exclude: 0.5000
  role_hint: 0.5000

# cpv_prefixes (tolerance=0.50, snapped to step 0.4)
cpv_prefixes:
  - "4531"  # Electrical installation work
  - "45300000"  # Building installation work

# nuts_prefixes (tolerance=0.50, snapped to step 0.4)
nuts_prefixes:
  - "DE3"  # Berlin
  - "DE4"  # Brandenburg
  - "DE5"  # Bremen
  - "DE6"  # Hamburg
  - "DE7"  # Hessen
  - "DE8"  # Mecklenburg-Vorpommern
  - "DE9"  # Niedersachsen
  - "DEA"  # Nordrhein-Westfalen
  - "DEF"  # Schleswig-Holstein

# value_min / value_max (tolerance=0.50, interpolated between
# tight [0, 783,834] and
# loose [0, 7,838,340])
value_min: 0
value_max: 4311087

# exclude_keywords (tolerance=0.50, snapped to step 0.4)  # NOTE: tolerance > 0 here means real stated exclusions are being dropped.
exclude_keywords:
  - "Gleisbau"

# role_hint_reject (tolerance=0.50, snapped to step 0.4)
role_hint_reject:
  - "Elektroinstallation"
  - "Elektroarbeiten"

# Newer optional fields (procurement_methods_allowed, min_bid_prep_days,
# reject_reserved_participation, contract_starts_after/_before) are left
# unset here -- company_details.json doesn't carry enough signal to set
# them confidently. Uncomment and tune by hand once real match volume exists.
# procurement_methods_allowed: ["open", "restricted"]
# min_bid_prep_days: 10
# reject_reserved_participation: false
# contract_starts_after: null
# contract_starts_before: null

notes: "Filter for an electrical installation contractor operating across Northern Germany (HB, HH, MV, NI, SH) with projects up to ~784k EUR. [Generated at tolerance: cpv=0.50, nuts=0.50, value=0.50, exclude=0.50, role_hint=0.50]"
